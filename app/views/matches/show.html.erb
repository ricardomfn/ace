<div class="container">
  <div class="banner">
  <h1><%= @match.address %></h1>
  <h2><%= @match.date%>
  </div>

  <div class="match-infos">
    <h2><strong>Détails</strong></h2>

    <div class="user-infos">
      <%= cl_image_tag @match.user.photo.key%>
      <%= @match.user.nickname%>
    </div>


    <%= @match.type%>
    <%= @match.modality%>
    <%= @match.price%>
    <%= @match.level%>
    </div>

    
    <div data-controller="popup">
    <button class="btn btn-primary" id="myButton">Ace!</button>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>

    <div class="map"
        data-controller="map"
        data-map-marker-value="<%= @marker.to_json %>"
        data-map-api-key-value="<%= ENV['MAPBOX_API_KEY'] %>">
    </div>
  </div>
